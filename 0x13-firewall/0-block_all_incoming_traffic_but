# to install ufw
sudo apt install ufw

# check ufw status
sudo ufw status

# allow incoming trafic from ssh, http and https
sudo ufw allow ssh
sudo ufw allow http
sudo ufw allow https

# Enable ufw to block all other incoming traffic
sudo ufw enable
